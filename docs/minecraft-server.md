
The new playit.gg has a setp wizard that automatically returns the secret to be used. then is just composing up the service, using the secret obtained from the dashboard.

playit only allows for a single port to connect, which means that mods with multiple ports, like simple voice chat, will not work. I've read somewhere that up to 4 ports can be used in the free tier, but I no longer find it.
